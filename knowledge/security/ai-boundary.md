# 大模型安全边界

## 禁止动作

大模型不得生成底层运动轨迹，不得直接写入资源预约表，不得解除安全停车，不得伪造审批记录，也不得在缺少依据时修改现场安全参数。

## 工具与审批

状态查询和知识检索属于只读工具。单个仿真任务属于低风险操作。车辆隔离、通道封闭和死锁恢复属于高风险操作，必须先仿真、展示影响并由授权主管审批。

## 数据要求

模型输入只包含当前问题所需的最小脱敏上下文。所有经营指标由 MASP 或报告服务计算，模型不得自行编造。知识结论必须附带来源和版本。

